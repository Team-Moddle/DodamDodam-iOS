import SwiftUI

struct ProfileView: View {
    var body: some View {
        Text("프로필 화면입니다.")
    }
}

struct ProfileView_Previews: PreviewProvider {
    static var previews: some View {
        ProfileView()
    }
}
